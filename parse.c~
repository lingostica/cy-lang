#include "token.h"
#include <stdio.h>

extern token_t lex (char *s);

token_t
parse (char *s)
{
  token_t token;

      token = lex (s);
      printf ("%d %s\n", token.type, token.lexeme);

  return token;
}
